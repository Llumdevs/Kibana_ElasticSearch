PUT _scripts/buscar_general
{
  "script": {
    "lang": "mustache",
    "source": {
      "query": {
        "multi_match": {
          "query": "{{texto}}",
          "fields": ["titulo", "autor", "editorial"]
        }
      }
    }
  }
}